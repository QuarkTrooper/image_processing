
# ************************************************
# Function work in progress
# ************************************************

def gaussian_blur_alex(img_in):
    print("Adding gaussian blur...")
    img_filtered = img_in

    imWidth = len(img_filtered[0, :])
    imHeight = len(img_filtered[:, 0])

    print("mod image width: " + str(imWidth))
    print("mod image height: " + str(imHeight))

    # Perform convolution
    # BLABLABLA

    return img_filtered